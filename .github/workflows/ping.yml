name: Keep Supabase Function Warm

on:
  schedule:
    - cron: '*/5 * * * *'  # every 5 minutes UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Health Check
        run: |
          curl -s https://zwqsnvuhsokcvolgwkid.functions.supabase.co/super-handler/health || echo "Ping failed"
